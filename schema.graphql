type Whitepaper @entity {
  id: ID!
  owner: Bytes!
  paper: String
  paperContent: String
  isEdited: Boolean!
}

type Transfer @entity {
    id: ID!
    from: Bytes!
    to: Bytes!
    tokenId: BigInt!
}

type AppData @entity {
    id: ID!
    numMinted: Int!
    numEdited : Int!
}